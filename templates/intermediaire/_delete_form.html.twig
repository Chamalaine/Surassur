<form method="post" action="{{ path('intermediaire_delete', {'id': intermediaire.id}) }}" onsubmit="return confirm('Êtes-vous sur de vouloir supprimer votre compte ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ intermediaire.id) }}">
    <button class="btn btn-danger">Supprimer le compte</button>
</form>
